/**
 * SendPost API
 * Email API and SMTP relay to not just send and measure email sending, but also alert and optimise. We provide you with tools, expertise and support needed to reliably deliver emails to your customers inboxes on time, every time.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: hello@sendpost.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ModelsIP from './ModelsIP';
import ModelsThirdPartySendingProvider from './ModelsThirdPartySendingProvider';

/**
 * The ModelsAccountIPPool model module.
 * @module model/ModelsAccountIPPool
 * @version 1.0.0
 */
class ModelsAccountIPPool {
    /**
     * Constructs a new <code>ModelsAccountIPPool</code>.
     * @alias module:model/ModelsAccountIPPool
     */
    constructor() { 
        
        ModelsAccountIPPool.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ModelsAccountIPPool</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ModelsAccountIPPool} obj Optional instance to populate.
     * @return {module:model/ModelsAccountIPPool} The populated <code>ModelsAccountIPPool</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ModelsAccountIPPool();

            if (data.hasOwnProperty('created')) {
                obj['created'] = ApiClient.convertToType(data['created'], 'Number');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('ips')) {
                obj['ips'] = ApiClient.convertToType(data['ips'], [ModelsIP]);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('routingMetaData')) {
                obj['routingMetaData'] = ApiClient.convertToType(data['routingMetaData'], 'String');
            }
            if (data.hasOwnProperty('routingStrategy')) {
                obj['routingStrategy'] = ApiClient.convertToType(data['routingStrategy'], 'Number');
            }
            if (data.hasOwnProperty('thirdPartySendingProviders')) {
                obj['thirdPartySendingProviders'] = ApiClient.convertToType(data['thirdPartySendingProviders'], [ModelsThirdPartySendingProvider]);
            }
            if (data.hasOwnProperty('toAccountIPPools')) {
                obj['toAccountIPPools'] = ApiClient.convertToType(data['toAccountIPPools'], [ModelsAccountIPPool]);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], Object);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ModelsAccountIPPool</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ModelsAccountIPPool</code>.
     */
    static validateJSON(data) {
        if (data['ips']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['ips'])) {
                throw new Error("Expected the field `ips` to be an array in the JSON data but got " + data['ips']);
            }
            // validate the optional field `ips` (array)
            for (const item of data['ips']) {
                ModelsIP.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['routingMetaData'] && !(typeof data['routingMetaData'] === 'string' || data['routingMetaData'] instanceof String)) {
            throw new Error("Expected the field `routingMetaData` to be a primitive type in the JSON string but got " + data['routingMetaData']);
        }
        if (data['thirdPartySendingProviders']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['thirdPartySendingProviders'])) {
                throw new Error("Expected the field `thirdPartySendingProviders` to be an array in the JSON data but got " + data['thirdPartySendingProviders']);
            }
            // validate the optional field `thirdPartySendingProviders` (array)
            for (const item of data['thirdPartySendingProviders']) {
                ModelsThirdPartySendingProvider.validateJSON(item);
            };
        }
        if (data['toAccountIPPools']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['toAccountIPPools'])) {
                throw new Error("Expected the field `toAccountIPPools` to be an array in the JSON data but got " + data['toAccountIPPools']);
            }
            // validate the optional field `toAccountIPPools` (array)
            for (const item of data['toAccountIPPools']) {
                ModelsAccountIPPool.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {Number} created
 */
ModelsAccountIPPool.prototype['created'] = undefined;

/**
 * @member {Number} id
 */
ModelsAccountIPPool.prototype['id'] = undefined;

/**
 * @member {Array.<module:model/ModelsIP>} ips
 */
ModelsAccountIPPool.prototype['ips'] = undefined;

/**
 * @member {String} name
 */
ModelsAccountIPPool.prototype['name'] = undefined;

/**
 * @member {String} routingMetaData
 */
ModelsAccountIPPool.prototype['routingMetaData'] = undefined;

/**
 * @member {Number} routingStrategy
 */
ModelsAccountIPPool.prototype['routingStrategy'] = undefined;

/**
 * @member {Array.<module:model/ModelsThirdPartySendingProvider>} thirdPartySendingProviders
 */
ModelsAccountIPPool.prototype['thirdPartySendingProviders'] = undefined;

/**
 * @member {Array.<module:model/ModelsAccountIPPool>} toAccountIPPools
 */
ModelsAccountIPPool.prototype['toAccountIPPools'] = undefined;

/**
 * @member {Object} type
 */
ModelsAccountIPPool.prototype['type'] = undefined;






export default ModelsAccountIPPool;

